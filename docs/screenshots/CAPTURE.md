# Screenshots to add

The README references `docs/screenshots/hub.png`. Drop screenshots here (on macOS,
`Cmd+Shift+4` then `Space` to capture a single window cleanly). Recommended set:

- **`hub.png`** *(referenced by the README hero)* — the main "Grandma's Desktop" window with the
  floating widget docked top-right.
- `live-reaction.png` — the frosted widget close up (tier + gauge + stats).
- `agent-team.png` — the per-project orchestration graph.
- `ledger.png` — the 7-day chart + top-offenders.

A short animated GIF (widget reacting as the window % climbs, or navigating the hub) makes the
strongest README hero if you want to record one (e.g. with Kap or `Cmd+Shift+5`).
